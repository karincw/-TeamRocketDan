using UnityEngine;

namespace Karin.DialogSystem
{
    public enum DialogPosition
    {
        UnderBar,
        OnHead
    }
    public enum Charactor
    {
        Player,
        Actor
    }
    public enum DialogType
    {
        Intro = 1,
        MochiCreate = 2,
        MochiMove = 3,
        Option = 4,
        Dictionary = 5,
        Outro = 6,
    }
}
